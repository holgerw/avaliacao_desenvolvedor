<div class="container-fluid">

  <div class="row">
    <div style="padding: 20px; background-color: #e1e1e8;">
      <h1 style="font-size: 28px;">Importação de Registros de Compra</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-6" style="padding: 20px; background-color: #f6f6f6;">
      <h2 style="font-size: 22px; color:#545454; margin-bottom: 20px;"> Carregamento de arquivo</h2>
      <%= form_for(@import) do |f| %>
        <div class="field">
          <%= f.file_field :file %>
        </div>
        <div class="actions" style="margin-top: 20px; margin-bottom: 40px;">
          <%= f.submit 'Efetuar carga' %>
        </div>
      <% end %>
      <div><i><strong>Ajuda:</strong> Escolhe um arquivo e confirme com o botão "Efetuar carga", para importar novos registros à base de compras.</i></div>
    </div>

    <div class="col-sm-6" style="padding: 20px;">
      <h2 style="font-size: 22px; color:#545454; margin-bottom: 20px;">Histórico de importações</h2>
      <table class="table table-striped">
        <tr>
          <th>Data</th>
          <th>Receita bruta total</th>
        </tr>
        <% @import_history.each do |import| %>
        <tr>
          <td><%= creation_date(import) %></td>
          <td><%= import.total_despense %></td>
        <tr>
        <% end %>
      </table>
    </div>
  </div>

</div>